# Netlify configuration for static site
# This is a static site with index.html at the root
[build]
  command = "npm run build"
  publish = "."

# SPA routing - redirect all routes to index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
